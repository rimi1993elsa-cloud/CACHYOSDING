package org.cachyos.controlcenter.core.action;

/** Trusted local sources. AI is intentionally not an action source. */
public enum InputSource {
  BUTTON,
  TEXT,
  VOICE,
  INTERNAL_SCHEDULED_REFRESH
}
